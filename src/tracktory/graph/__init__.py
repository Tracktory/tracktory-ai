"""tracktory LangGraph 정의 — state, 노드, 엣지.

구체 그래프 빌더(``StateGraph`` 조립)는 각 노드 구현이 진행되는 동안
점진적으로 추가된다. 본 패키지의 진입점은 ``tracktory.graph.state.GraphState``
이며, 노드는 ``tracktory.graph.nodes`` 하위 모듈에 위치한다.
"""
